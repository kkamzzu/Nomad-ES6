// value Shortthands(변수명 단축)

const follow = checkFollow();
const alert = checkAlert();

//변수 이름을 똑같이 하고싶다면 단축속성명을 사용할 수 있음 (follow:follow -> follow)
const settings = {
    notifications: {
        follow
    }
};